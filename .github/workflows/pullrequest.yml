name: Display Pull Request Info
on: [workflow_dispatch]

jobs:
  #checkout the repo
  #setup node
  #install dependencies
  #run module
  #get csv output from module
  #upload csv as artifact

  #need to modify the secrets
  # get_pull_request_info:
  #   runs-on: ubuntu-latest
  #   permissions:
  #     issues: read
  #   steps:
  #     - name: Check out repo content
  #       uses: actions/checkout@v4
  #       with:
  #         repository: ${{ github.repository }} #gets the current repo
  #         ref: ${{ github.ref }} #gets the current branch
  #         token: ${{ secrets.EF_TOKEN }}

  #     - name: Setup Node
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '18'
  #         cache: npm

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Run script
  #       run: |
  #         echo "$(node scripts/get-pull-requests.js)" > scripts/pull_requests.csv
  #       env: #creates environment variable for js module
  #         TOKEN: ${{ secrets.EF_TOKEN }}

  #     - name: Debug
  #       run: |
  #         cat scripts/pull_requests.csv

  #     - name: Upload artifact
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: pull_request_info
  #         path: scripts/pull_requests.csv

  #     - name: Download artifact
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: pull_request_info
  #         path: scripts/pull_requests2.csv

  update-file:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Update Existing File
        run: |
          # Set up Git configuration (you may need to configure user.name and user.email)
          git config --global user.name "Sloane Tribble"
          git config --global user.email "sloane.tribble@liatrio.com"

          # Modify the existing file or replace it with the updated content
          # Example: echo "Hello from actions!" > README.md

          # Add, commit, and push the changes to the repository
          git add README.md
          git commit -m "Update README"
          git push --force https://engaging-finches:${{ secrets.PAT }}@github.com/engaging-finches/demo-pr-data.git main
